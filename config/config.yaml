http:
    addr: ":8080"
    username: admin
    password: cheese
    chartPath: "charts"

storage:
    enabled: true
    bucket_url: "s3://bucketrepo?endpoint=http://minio.minio-opeartor.svc.cluster.local&s3ForcePathStyle=true&region=ignored"

cache:
    base_dir: "/tmp/bucketrepo"

repositories:
    - url: "https://repo1.maven.org/maven2"
    - url: "http://uk.maven.org/maven2/"
